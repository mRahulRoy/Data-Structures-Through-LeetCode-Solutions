#include <iostream>
#include <vector>
#include <utility>
#include <algorithm>
using namespace std;
class Solution
{
    vector<int> arrayRankTransform(vector<int> &arr)
    {
        // vector<pair<int, int>> p;
        // int small = INT_MAX;
        // int count = 1;
        // for (int i = 0; i < arr.size(); i++)
        // {
        //     p.push_back(make_pair(arr[i],i));
        // }
        // sort(p.begin(),p.end());
        // for (int i = 0; i < arr.size(); i++)
        // {
        //     if(p[i].first == )
        // }
        return arr;
    }
};
int main()
{
    //  __builtin_popcount(argument); This is an inbuilt function used in counting the set bits of an number
    // example -- binary of 4 is 100 so setBits is 1 (sets bits is the ammount of 1s present in the binary number);
    // example -- binary of 5 is 101 so setsBits is 2 as two 1s is present in the 101
   

    return 0;
}